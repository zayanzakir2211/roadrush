{
  "name": "multiplayer-car-game-backend",
  "version": "1.0.0",
  "type": "module",
  "description": "Cloudflare Workers backend for multiplayer car game",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.90.1"
  }
}
